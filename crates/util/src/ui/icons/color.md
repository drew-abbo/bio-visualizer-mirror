# Changing Icon Color

To change the color of an icon:
1. Open the SVG in a text editor (SVGs are just XML documents).
2. Search for `fill` and change the value.
3. Convert the SVG to a PNG with an online converter (make sure you resize it to
   64x64).
